services:
  psf-guard:
    image: ghcr.io/theatrus/psf-guard:latest
    container_name: psf-guard
    volumes:
      - /share/Container/psf-guard/data:/data/database.sqlite
      - /share/Astrophotography/Repository:/images:ro
    ports:
      - 3000:3000
    restart: unless-stopped
